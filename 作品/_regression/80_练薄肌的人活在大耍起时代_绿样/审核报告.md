# 审核报告

body_path: `正文.md`
body_text_sha256: `a915b354cbee2543fc1e5cd7f0f5274ca5485af0df61217066fd913f1ea50a0d`
review_status: PROVISIONAL

说明：这是当前连续性/版本绑定回归夹具的绿样。它只验证当前已经落地的机械检查与审核报告绑定要求，不代表完整创作审稿已经自动完成。

2026-09-23 修正：`body_path` 原为作者本机的 Windows 绝对路径
（`C:\Users\29471\Desktop\git\demo\作品\_regression\...\正文.md`）。`scripts/fuben_loop.py`
的 `_normalize_report_body_path` 会把绝对路径直接 `resolve()`，在任何非该机器上都无法与当前正文对齐，
绿样因此恒判 `STALE_REVIEW_BODY_PATH`，`tests/test_fuben.py` 的
`test_regression_work_80_red_and_green_fixtures_cover_current_scope` 长期失败。
改为**报告目录相对路径**，绑定校验跨平台可复现；哈希不变（正文未改）。
写审核报告时一律用相对 `body_path`，不要写本机绝对路径。
