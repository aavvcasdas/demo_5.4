# AGENTS.md — 代理路由

本仓库只有两条路：**写新剧本** 与 **查旧账**。别走错。

## 写剧本 / 改剧本 / 扒片 / 改编 / 蒸馏

→ 读 [`剧本工厂/SKILL.md`](剧本工厂/SKILL.md)，按 G0-G6 执行。只加载本次需要的那一个 reference，**不要把 8 个 reference 全读一遍再动笔**（那是旧链路的病）。

| 你要做的事 | 只读这个 |
|---|---|
| 判断这条片子算哪种 | `剧本工厂/references/判型卡.md` |
| 选/换骨架 | `剧本工厂/references/骨架库.md` |
| 想开场 | `剧本工厂/references/钩子库.md` |
| 写句子 | `剧本工厂/references/口播语法.md` |
| 排加价与兑现 | `剧本工厂/references/兑现与加价.md` |
| 扒对标、出指纹 | `剧本工厂/references/结构指纹协议.md` |
| 系列化改编 | `剧本工厂/references/高保真改编.md` |
| 交付前检查 | `剧本工厂/references/验收表.md` + `python3 剧本工厂/scripts/fuben_check.py <剧本>` |

**硬门禁（违反即返工，不看心情）**：
1. 没有 `结构指纹.json` 不许开写正文；
2. 台词占口播字数 <25% → 重写；<35% → 警告；
3. `fuben_check.py` 出 BLOCK → 修完重跑；正文一改，旧体检作废；
4. 旁白代替人物打架、日历句、裸数字、AI 腔硬禁词 → 直接改掉。

## 研究 / 回顾 / 找教训

→ 读 [`docs/诊断_剧本为什么水.md`](docs/诊断_剧本为什么水.md) 与 [`docs/GitHub来源评估.md`](docs/GitHub来源评估.md)。
→ `archive/旧链路/` 是冻结资产：可以引用、可以对照，**不要再往里面写入，也不要把它的流程当成现行规程**。

## 语料

`拆文库/` 是只读语料。要用它，跑 `python3 剧本工厂/scripts/distill.py`（产物在 `剧本工厂/资产/`），不要手工去读 54 篇 1.4 万字的拆文报告——那是旧链路的读法。

## 用户说“不好看”时

优先级：**开头 → 加价 → 兑现 → 台词**。不许转去调格式、跑工具、加配图、补充说明文档。
每轮返工最多两轮；两轮还不成立，换骨架重来，不要在句子上磨。
